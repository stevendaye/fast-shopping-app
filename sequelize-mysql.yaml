dbname: fastshopping
username: root
password: root
params:
  dialect: mysql
  host: localhost
  port: 3306
  operatorAliases:
    $eq: Op.eq
    $ne: Op.ne
    $gte: Op.gte
    $gt: Op.gt
    $lte: Op.lte
    $lt: Op.lt
    $or: Op.or